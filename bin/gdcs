#!/bin/bash

gdm=$(git diff $@ --name-only --diff-filter=AM);

for f in $gdm; do 
	echo $f;
	cs $f;
	response=$(read -p "(r)echeck, (q)uit, any other key to continue..." -n1 -s);
	if $response == "q"
		exit;
	fi
done
